using System;

namespace Zoolandia.animals
{
    public class Bird : Animal
    {
        public bool CanFly { get; 
        if (CanFly) {
            return "can";
        }
        else
        {
           return "can not"; 
        }
        }
    }
}